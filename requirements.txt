# Core Data Processing
pyspark==3.5.0
pandas==2.1.4
numpy==1.26.2

# AWS Integration
boto3==1.34.10
botocore==1.34.10

# Python Standard Library (included but listed for clarity)
# - No installation needed, but documenting for reproducibility
# os
# sys

# Optional: For local development and debugging
# matplotlib==3.8.2
# seaborn==0.13.0

# Note: The following are NOT included in requirements.txt
# as they require system-level installation:
# - Java 17 (OpenJDK-17-JDK-Headless) - Install via: sudo apt install openjdk-17-jdk-headless
# - Apache Spark 3.5 - Bundled with PySpark above
# - AWS CLI - Install via: pip install awscli (if not already installed)